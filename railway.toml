# Root railway.toml — Railway uses per-service rootDirectory set in the dashboard.
# Each service (backend, demo, app, internal, website) points to its own subdirectory.
# This file is kept for reference; per-service configs live in each subdirectory.
