[build]
builder = "DOCKER"

[deploy]
startCommand = "uvicorn app.main:app --host :: --port 3002"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.watchPaths]]
path = "harness/**"
